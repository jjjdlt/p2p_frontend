import canUseDOM from "../../modules/canUseDOM";
function _instanceof(left, right) {
  return right != null && typeof Symbol < "u" && right[Symbol.hasInstance] ? !!right[Symbol.hasInstance](left) : left instanceof right;
}
function createCSSStyleSheet(id, rootNode, textContent) {
  if (canUseDOM) {
    var root = rootNode ?? document, element = root.getElementById(id);
    if (element == null)
      if (element = document.createElement("style"), element.setAttribute("id", id), typeof textContent == "string" && element.appendChild(document.createTextNode(textContent)), _instanceof(root, ShadowRoot))
        root.insertBefore(element, root.firstChild);
      else {
        var head = root.head;
        head && head.appendChild(element);
      }
    return element.sheet;
  } else
    return null;
}
export {
  createCSSStyleSheet as default
};
//# sourceMappingURL=createCSSStyleSheet.js.map
