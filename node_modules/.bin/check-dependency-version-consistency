#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../check-dependency-version-consistency/dist/bin/check-dependency-version-consistency.js" "$@"
  ret=$?
else 
  node  "$basedir/../check-dependency-version-consistency/dist/bin/check-dependency-version-consistency.js" "$@"
  ret=$?
fi
exit $ret
