#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../../../build/teesx.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../../../build/teesx.sh" "$@"
  ret=$?
fi
exit $ret
